"use strict";

var _testUtils = require("@vue/test-utils");

var _ = require(".");

describe('===== Alert Component Tests =====', function () {
  var alert;
  beforeEach(function () {
    alert = (0, _testUtils.shallowMount)(_.Alert, {
      propsData: {
        id: 123,
        title: 'Test notification',
        close: jest.fn(),
        clear: jest.fn(),
        reset: jest.fn()
      }
    });
  });
  describe('Instance tests', function () {
    it('should be a Vue instance', function () {
      expect(alert.isVueInstance()).toBeTruthy();
    });
  });
});